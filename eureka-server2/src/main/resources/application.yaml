server:
  port: 8764
spring:
  application:
    name: eureka-server2
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/ #注册到另一个Eureka注册中心